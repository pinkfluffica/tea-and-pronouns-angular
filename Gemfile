source "https://rubygems.org"

# Tiny web framework
gem "sinatra"

# SQL database toolkit
gem "sequel"

group :development, :test do
  # Use sqlite3 database in development and test
  gem "sqlite3"
end

group :production do
  # Use Postgresql database in production
  gem "pg"
end
